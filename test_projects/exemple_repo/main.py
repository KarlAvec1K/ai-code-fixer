# main.py

def factorial(n):
    if n == 0:
        return 1
    else
        return n * factorial(n - 1)  # SyntaxError: Missing colon

def divide(a, b)
    return a / b  # SyntaxError: Missing colon

def is_even(n: int) -> bool:
    if n % 2 = 0:  # SyntaxError: Assignment operator used instead of comparison
        return "yes"  # Logical error: Should return a boolean, not a string
    return "no"  # Logical error: Should return a boolean, not a string